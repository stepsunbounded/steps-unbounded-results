# R003 — Direct powers of A₅ satisfy Herzog–Schönheim

**Status:** Lean verified, subject to this repository's CI and axiom audit

## Statement

The formal development proves the Herzog–Schönheim property for direct powers of `A₅`, together with the associated product/quotient extensions represented by the audited headline theorems below.

The proof uses the subgroup-index mass criterion and an explicit bound for the multiplicative monoid generated by the subgroup indices of `A₅`.

## Formal verification

Primary Lean facade:

- [`lean/StepsUnboundedResults/R003.lean`](../lean/StepsUnboundedResults/R003.lean)

Shared HSC development:

- [`lean/StepsUnboundedResults/HSC/`](../lean/StepsUnboundedResults/HSC/)

Audited headline declarations:

- `HSC.massBound_a5IndexSet`
- `HSC.isHS_pow_Alt5`
- `HSC.isHS_pow_Alt5_prod_pow_C5`
- `HSC.isHS_quotient_pow_Alt5_prod_pow_C5`

The implementation is intentionally shared rather than duplicated per result: the general definitions, reciprocal-mass criterion, extension/product lemmas, `A₅` subgroup-index description, and monoid bounds live in the HSC module tree.

## Independent computational check

- [`verify/R003.py`](../verify/R003.py)

Run from the repository root:

```bash
python3 verify/R003.py
```

The script independently reconstructs the `A₅` subgroup-index monoid, checks the closed-form reciprocal mass in exact rational arithmetic, and cross-checks it by enumeration plus a tail bound. It corroborates the arithmetic layer; the Lean development verifies the formal theorem chain.

## Verification contract

The formal claim is accepted only when CI successfully runs:

```bash
cd lean
lake exe cache get
lake build StepsUnboundedResults
lake env lean AxiomAudit.lean
```

The canonical sources are also scanned to reject `sorry`, `admit`, and explicit `axiom` declarations.

## Website presentation

<https://stepsunbounded.com/results/R003/>

The website is the readable presentation. This record and the linked Lean sources are the canonical technical archive for the formal claim.
